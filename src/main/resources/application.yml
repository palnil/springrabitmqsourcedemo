server:
  port: 8087
spring:
  cloud:
    stream:
      defaultBinder: rabbit
      bindings:
        single-message:
          destination: single.message.topic
          contentType: application/json
